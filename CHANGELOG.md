# 变更记录

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

---

## [2.0.0] — 2026-09-16

**首次公开发布。** 此前是本机自用的原型，没有公开过；这个版本做了完整的重写、
系统性的复盘优化，并补齐了测试、打包脚本与合规文档。

版本号从 2.0.0 起算，是因为它在作者自己的迭代里是第二代实现（第一代是个
单文件脚本，未公开、也不再维护）。

### 新增

**界面与搜索**
- tkinter 图形界面，跟随系统浅色/深色主题，三页布局（搜索 / 历史 / 设置）。
- 搜索条件一行摆开：关键词 + 分类 + 分辨率 + 比例 + 排序。
- 「自适应」分辨率：取所有显示器中最大的宽 × 高，双屏不会漏掉大屏。
- 无限滚动翻页，最多预取 2 页；随机换一张。

**预览**
- 右侧面板先秒出缩略图、后台再换成原图（超过 25MB 的自动跳过，保留缩略图）。
- 滚轮以**鼠标位置为锚点**缩放、左键拖拽平移、双击在「适应窗口 ↔ 1:1」间切换，
  最高 12×。
- 「弹出大图」独立窗口，最大 82% 屏幕，键盘 `+` `-` `0` `1` `Esc` 全支持。

**下载与历史**
- 下载记录、重复命中直接复用本地文件、已下载角标。
- 历史页按关键词分组、增量刷新、失效记录批量清理。
- 「重新下载」：本地文件被删后按原图直链找回；老记录没有直链时自动回查一次 API。
- **断点续传**：下载中断后不从头再来，已下到的部分留在 `.part` 里，
  下次带上 `Range` 从断点接着下。
- **可中断的原图预览**：切到别的图时立刻放弃正在下的那张，连连接都不开。

**设置与迁移**
- 保存目录、命名规则、填充方式、代理模式、缩略图缓存上限、默认关键词、
  界面主题、NSFW 开关与 API Key。
- 换保存目录时自动搬文件并更新记录；同名不覆盖（大小一样只改记录，
  不一样则改名成 `xxx_1.jpg`）；文件已丢失或已挪走的跳过并分别报数。

**命令行**
- `--random` / `--silent` / `--keyword` / `--version` 等，供计划任务调用。

**打包**
- PyInstaller 单文件；图标由 `make_icon.py` 用 Pillow 程序化生成（无第三方素材）。
- exe 带版本资源，右键「属性 → 详细信息」能看到版本号。

### 工程质量

- 依赖清单：`requirements.txt`（运行时）+ `requirements-dev.txt`（加 pyinstaller / ruff）。
- `ruff.toml` + GitHub Actions CI（lint + 单测 + `--version` 冒烟）。
- 三个测试脚本：`unit_test.py`（214 项纯逻辑断言，约 1 秒）、
  `feature_test.py`（152 项离线 + 2 节联网）、`smoke_test.py`（打包产物冒烟）。
- `app/tools/fakes.py`：假客户端 / 假下载器，让「重新下载」这条分支最多的路径
  在离线环境也能跑。

### 设计取舍

几个刻意做出的选择，记下来免得以后又改回去：

- **只用官方 API，不解析网页**。爬 HTML 的写法在站点改版后会直接失效，
  而且会给对方服务器带来无谓的负载。API 有明确的速率限制（45 次/分钟），
  客户端这边做了指数退避重试和 429 的 `Retry-After` 处理。
- **不依赖 pywin32**，改用标准库 `ctypes` + `winreg`。少一个依赖，
  打包时也少一堆坑。
- **默认「填充」而不是「拉伸」**。拉伸在非原生分辨率下会把图拉变形。
- **缩略图的解码与缩放放在工作线程**。放在主线程时，一页 24 张图几乎同时到达
  会让翻页明显卡顿。
- **原图预览限制的是像素数，不是文件体积**。一张 8192×4606 的 PNG 可能只有几 MB，
  解码出来是 150MB，缓存 3 张就是 450MB。
- **`.part` 文件名里带下载地址的指纹**。换地址后旧的那半截数据跟新地址对不上，
  拼出来是个坏文件；带指纹之后它天然是另一个文件名，用不上。

### 文档

- `README.md`、`CHANGELOG.md`
- [`LICENSE`](LICENSE)（MIT）、[`DISCLAIMER.md`](DISCLAIMER.md)、
  [`THIRD-PARTY-NOTICES.md`](THIRD-PARTY-NOTICES.md) + `third_party_licenses/`

---

## 关于版本号

`APP_VERSION` 只有 `app/config.py` 一个来源，窗口标题、启动日志、`--version`
和 exe 的版本资源都从它派生（exe 的版本资源由 `app/tools/make_version_file.py`
生成，打包时用 `--version-file` 传进去）。改版本号只需要改那一处。
